function setup() {
  createCanvas(400,400);
  background("green");
}

function draw() {
  
  stroke("black");
  fill("yellow");
  
//console.log(mouseisPressed);
  
  if(mouseIsPressed) {
    rect(mouseX, mouseY, 20,35);
    
  }
}

